# 项目架构说明

## 轻量级本地 MCP 网关与多 Agent 调度系统

> 对标 OpenClaw/Trae 核心能力，自研 MCP 网关 + Agent 调度 + 性能优化

---

## 项目结构

```
LLM/
├── mcp_gateway/                  # MCP 网关层
│   ├── protocol.py              # MCP 协议实现（JSON-RPC 2.0）
│   ├── server.py                # 网关服务器入口
│   ├── transport.py             # HTTP/SSE/REST 传输层
│   ├── security.py              # 认证 + 限流 + 权限
│   ├── tenancy.py               # 多租户管理
│   ├── api.py                   # REST API 层（Dify/Ollama 兼容）
│   ├── audit.py                 # 审计日志
│   ├── tools/                   # 内置工具
│   │   ├── filesystem.py        # 文件系统工具（5个）
│   │   ├── terminal.py          # 终端命令工具（3个）
│   │   ├── database.py          # 数据库工具（5个）
│   │   ├── web.py               # 网页工具（3个）
│   │   └── llm.py               # Ollama 大模型工具（3个）
│   ├── agents/                  # Agent 调度层
│   │   ├── state.py             # 状态定义 + 快照管理
│   │   ├── retry.py             # 重试 + 熔断机制
│   │   ├── graph.py             # LangGraph 工作流编排
│   │   ├── supervisor.py        # Supervisor-Worker 模式
│   │   ├── planner.py           # 规划 Agent（Task→SubTask）
│   │   ├── executor.py          # 执行 Agent（Tool→Result）
│   │   └── validator.py         # 校验 Agent（Result→Score）
│   ├── rag/                     # RAG 知识库
│   │   └── vector_store.py      # ChromaDB + Milvus
│   └── llm/vllm_adapter/        # vLLM 适配
│       └── server.py            # vLLM + Ollama 进程管理
│
├── performance/                  # 性能优化层
│   ├── cache.py                 # 增量上下文缓存
│   ├── parallel.py              # 并行调度 + 依赖图
│   └── adapter.py               # 多模型适配器
│
├── config/                       # 配置加载
├── core/                         # 基础设施（类型、异常、接口）
├── requirements/                 # 依赖管理
│   ├── runtime.txt              # 运行依赖
│   ├── dev.txt                  # 开发依赖
│   └── default.txt              # 全部依赖
├── docker/                       # Docker 部署
│   ├── Dockerfile
│   └── docker-compose.yml       # 多服务编排
├── tests/                        # 测试
│   ├── test_mcp.py              # MCP 层测试
│   ├── test_agent.py            # Agent 层测试
│   ├── test_integration.py      # 集成测试
│   └── benchmark.py             # 跑分脚本
├── examples/                     # 集成示例
│   ├── integration_demo.py      # MCP + REST API + Ollama
│   └── demo_agent.py            # Agent 工作流演示
├── scripts/                      # 工具脚本
│   ├── setup_mcp.py             # IDE 一键接入
│   └── build_exe.py             # PyInstaller 打包
├── docs/                         # 文档
│   ├── MCP协议规范与实践.md
│   ├── A2A协议规范与实战.md
│   ├── LangGraph实战指南.md
│   ├── 性能优化与跑分.md
│   └── ...
├── main.py                       # 主入口
├── pyproject.toml                # 项目元数据
└── .gitignore
```

---

## 架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户 / AI 应用                            │
└─────────────────────────────┬───────────────────────────────────┘
                              │ JSON-RPC 2.0 (STDIO / HTTP)
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      MCP 网关层 (mcp_gateway)                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │ 文件系统工具   │  │ 终端命令工具   │  │ 数据库工具    │           │
│  │ fs_read_file  │  │ term_run     │  │ db_query     │           │
│  │ fs_write_file │  │ term_sysinfo │  │ db_list      │           │
│  │ fs_list_dir   │  │ term_python  │  │ db_describe  │           │
│  │ fs_search     │  │              │  │ db_create    │           │
│  │ fs_get_info   │  │              │  │ db_insert    │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │                    工具注册中心 (ToolRegistry)              │    │
│  │  • 动态注册/注销  • 依赖图管理  • 并行调度识别              │    │
│  └──────────────────────────────────────────────────────────┘    │
└─────────────────────────────┬───────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   Agent 调度层 (agent_scheduler)                   │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │              Supervisor Agent (中心协调者)                  │    │
│  │          • 动态路由  • Handoffs  • 流式输出                │    │
│  └──────────┬──────────────────┬──────────────────┬─────────┘    │
│             │                  │                  │               │
│             ▼                  ▼                  ▼               │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐        │
│  │ 规划 Worker   │───►│ 执行 Worker   │───►│ 校验 Worker   │        │
│  │ (Planner)    │    │ (Executor)   │    │ (Validator)  │        │
│  │              │    │              │    │              │        │
│  │ 意图分析      │    │ 依赖分析      │    │ 结果检查      │        │
│  │ 任务拆解      │    │ 并行调度      │    │ 质量评分      │        │
│  │ 依赖标注      │    │ 重试熔断      │    │ 建议生成      │        │
│  └──────────────┘    └──────────────┘    └──────────────┘        │
│         │                    │                    │               │
│         └──────── 失败/重试 ← ┘                    │               │
│                            校验不通过 ──────────────┘               │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │                 LangGraph 工作流引擎                       │    │
│  │  • 状态管理  • 条件路由  • 断点续跑  • 子图拆分           │    │
│  └──────────────────────────────────────────────────────────┘    │
└─────────────────────────────┬───────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    性能优化层 (performance)                        │
│                                                                   │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐   │
│  │ 增量上下文缓存     │  │ 并行调度器        │  │ 多模型适配器  │   │
│  │                  │  │                  │  │              │   │
│  │ • 内容哈希去重    │  │ • 依赖图分析      │  │ • DS-V4 API  │   │
│  │ • 智能压缩截断    │  │ • 拓扑分层        │  │ • Ollama本地 │   │
│  │ • LRU 淘汰       │  │ • 并发控制        │  │ • vLLM 推理  │   │
│  │                  │  │                  │  │              │   │
│  │ Token -49.5%    │  │ 耗时 -65.0%      │  │ 统一接口     │   │
│  └──────────────────┘  └──────────────────┘  └──────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 核心数据流

```
1. 用户输入 → Planner Agent（拆解为子任务 DAG）
2. 子任务 DAG → Executor Agent（依赖分析 + 并行调度）
3. 工具调用 → MCP Gateway（协议路由 + 执行）
4. 工具结果 → 增量缓存（去重压缩）
5. 执行结果 → Validator Agent（质量校验）
6. 校验通过 → 返回最终结果
   校验失败 → 返回 Planner 重新规划（最多 3 次）
```

---

## 核心指标

| 指标 | 数值 | 说明 |
|------|------|------|
| 内置工具数 | 14 个 | 文件系统(5) + 终端(3) + 数据库(5) |
| Token 压缩率 | 49.5% | 增量上下文缓存 |
| 并行加速比 | 2.9x | 6 工具并行 vs 串行 |
| 耗时降低 | 65.0% | 无依赖工具并行调度 |
| Agent 重试次数 | 3 次 | 指数退避 + 熔断保护 |
| Docker 服务数 | 7 个 | MCP 网关 + Ollama + ChromaDB + Milvus |

---

## 快速启动

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 启动 MCP 网关
python main.py --host 0.0.0.0 --port 9090

# 3. 运行演示
python main.py --demo

# 4. 运行跑分
python main.py --benchmark

# 5. 运行测试
python main.py --test

# 6. Docker 一键部署
cd docker && docker-compose up -d
```

---

## 技术栈

- **协议**: MCP (Model Context Protocol) + JSON-RPC 2.0
- **编排**: LangGraph (StateGraph + Checkpoint)
- **LLM**: DeepSeek-V4 API / Ollama (Qwen2.5, Llama3.1) / vLLM
- **向量库**: ChromaDB / Milvus
- **部署**: Docker Compose (7 服务编排)
- **测试**: pytest + asyncio
- **语言**: Python 3.11+ (全异步)